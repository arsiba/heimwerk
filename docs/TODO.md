# Heimwerk TODOs – Deployment & Features

## 1. Deployment Views

* Add views for managing `Instance` deployments. 
* Add permission checks so users only see their own instances.

## 2. Deployment Forms

* Build forms to deploy instances based on Docker modules.
* Include generic form based on docker missing variables e.g. machine/host selection and domain/subdomain selection.

## 3. Docker Backend

* Implement basic deploy functionality.
* Capture outputs and instance status.
* Support destroying.

## 4. Settings & Secure Storage

* Store Docker hosts, credentials, configs, and state securely.
* Ensure access control and encryption for sensitive data.

## 5. Tests

* Write unit and integration tests for:
  * Deployment views
  * Deployment forms
  * Docker backend functionality
  * Settings and credentials handling
